name: Repo Health
on:
  pull_request:
    branches: [dev]
    types: [opened, synchronize]

jobs:
  repo_health:
    uses: OpenVoiceOS/gh-automations/.github/workflows/repo-health.yml@dev
    with:
      version_file: ovos_pydantic_models/version.py
    secrets: inherit
